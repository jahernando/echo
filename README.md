# echo

Compare two samples (e.g. train vs. test) and quantify how similar they are and where they differ.

## Layout

- `src/echo/` — package source
- `notebooks/` — exploratory Jupyter notebooks
- `tests/` — pytest tests
- `docs/` — documentation and notes

## Install (dev)

```bash
pip install -e ".[dev]"
```
